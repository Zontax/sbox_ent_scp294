@using Sandbox; @* TODO: blur effect dont work *@
@using Sandbox.UI;
@using System;
@using Bimbasic;
@attribute [StyleSheet];
<root>
	@if (isOpen)

	{
		<div class="active">
			BLUR EFFECT
		</div>
	}
</root>
@code

{
	bool isOpen = true;

	public int blurTime;

	void CloseBlur()

	{

		isOpen = false;

		//this.Delete();

		//drink.DeleteBlur();

	}

	public override void Tick()

	{

		Log.Error("Blur tick");

	}

	protected override int BuildHash()

	{

		return HashCode.Combine(isOpen);

	}
}
